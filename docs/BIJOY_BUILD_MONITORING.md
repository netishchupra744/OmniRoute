# Bijoy Build Monitoring

The pull request against `bijoy-v3.8.49-base` exists only to expose the Windows build run, jobs, logs, and artifacts through the connected GitHub integration. The application source remains based on OmniRoute v3.8.49.
